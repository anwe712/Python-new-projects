# this is a program that gives you the meaning of every word you type in
# it uses the json library to get the meaning of the word
# it uses the difflib library to get the closest word to the one you typed in
# it uses the get_close_matches function to get the closest word to the one you typed in
# it uses the len function to get the length of the word you typed in
# it uses the input function to get the word you typed in
# it uses the lower function to make the word you typed in lowercase
# it uses the upper function to make the word you typed in uppercase
# it uses the title function to make the word you typed in titlecase
# it uses the capitalize function to make the word you typed in capitalize
# it uses the split function to split the word you typed in